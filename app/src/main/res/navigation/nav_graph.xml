<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/nav_camera">

    <fragment
        android:id="@+id/nav_camera"
        android:name="com.example.prizoscope.ui.camera.CameraFragment"
        android:label="Camera"
        tools:layout="@layout/fragment_camera" />

    <fragment
        android:id="@+id/nav_settings"
        android:name="com.example.prizoscope.ui.settings.SettingsFragment"
        android:label="Settings"
        tools:layout="@layout/fragment_settings" />

    <fragment
        android:id="@+id/nav_map"
        android:name="com.example.prizoscope.ui.maps.MapFragment"
        android:label="Map"
        tools:layout="@layout/fragment_map" />

    <fragment
        android:id="@+id/nav_shopping"
        android:name="com.example.prizoscope.ui.shopping.ShoppingFragment"
        android:label="Shopping"
        tools:layout="@layout/fragment_shopping" />

    <fragment
        android:id="@+id/nav_bookmarks"
        android:name="com.example.prizoscope.ui.bookmarks.BookmarkFragment"
        android:label="Bookmarks"
        tools:layout="@layout/fragment_bookmarks" />
</navigation>
